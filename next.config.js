/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Alex-Rubio',
  assetPrefix: '/Alex-Rubio',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
